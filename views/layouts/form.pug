div(class="d-flex, flex-column, align-items-center")
    h3(class="mt-5") Carga de productos
    form(class="w-50, mt-5, text-left", method="POST", action="/productos")
        div(class="mb-3")
            label(for="title", class="form-label") Titulo
            input(type="text", class="form-control", id="title", name="title")
        div(class="mb-3")
            label(for="price" class="form-label") Precio
            input(type="text", class="form-control", id="price", name="price")
        div(class="mb-3")
            label(for="thumbnail", class="form-label") URL
            input(type="text", class="form-control", id="thumbnail", name="thumbnail")
        button(type="submit", class="btn btn-primary") Cargar